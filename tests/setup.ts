// ============================================
// ComposeYogi — Test Setup
// ============================================

// A real IndexedDB implementation for Node, so persistence code under test is
// the same code that runs in the browser.
import 'fake-indexeddb/auto';
